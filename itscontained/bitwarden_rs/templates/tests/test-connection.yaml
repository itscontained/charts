apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bitwarden_rs.fullname" . }}-test-connection"
  labels:
    {{- include "bitwarden_rs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bitwarden_rs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
